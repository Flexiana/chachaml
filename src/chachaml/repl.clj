(ns chachaml.repl
  "REPL-oriented helpers for chachaml.

  Conveniences such as `last-run`, `runs`, `inspect`, `compare-runs`
  and pretty printing for run/model maps. These are not required for
  programmatic use but make ad-hoc exploration painless.

  Implementation lands in M6.")
